/**
 * Functional tests to test learning session as a whole
 * No web layer is touched here, only service+database
 * We start, proceed to the end and finish learning sessions, persist results
 * and make sure that the correct results are in DB;
 */
package _functional;